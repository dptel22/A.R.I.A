# Frontend environment variables
# In development (via Vite proxy) and production (via Nginx), leave VITE_ARIA_API_URL empty
# so API calls route relatively without exposing API keys or hardcoding origins.
VITE_ARIA_API_URL=""
